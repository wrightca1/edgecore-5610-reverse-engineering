# Register info struct format (from soc_reg32_get_wrapper decomp):

struct reg_info {
    /* +0x00 */ uint32_t  reg_addr_or_block;   // SCHAN address + block-id encoded
    /* +0x04 */ uint32_t  ?;
    /* +0x08 */ uint32_t  ?;
    /* +0x0c */ uint32_t  ?;
    /* +0x10 */ uint32_t  flags;               // bit 0x100001 = >32-bit register
    /* +0x14 */ uint16_t  field_count;
    /* +0x16 */ uint16_t  ?;
    /* +0x18 */ struct {
                  uint32_t field_id;            // sorted ascending
                  uint16_t bit_offset;
                  uint16_t bit_width;
                  uint32_t ?;                   // 4 more bytes?
              } fields[field_count];
    /* end */
};

# Found in unit_state at offset 0x2f26f4 → some_table → [reg_id*4] → reg_info_ptr
# Each reg_info is on the heap (allocated by sysconf_attach).
# The unit_state struct base is at &DAT_1212b9c4[unit].
